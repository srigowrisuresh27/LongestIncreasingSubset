# LongestIncreasingSubset
## Steps to Run the Project

### 1. Clone the Repository
powershell : 
git clone https://github.com/<your-username>/<repo-name>.git
cd <repo-name>

### 2. Build the Image in PowerShell
docker build -t lisapp .

### 3 Check the output after the images are sucessfully created.
docker run -it --rm lisapp
